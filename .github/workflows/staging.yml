name: Deploy to Staging

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy-staging:
    runs-on: [self-hosted, mkair]
    environment: staging
    steps:
      - name: Checkout staging code
        run: |
          cd ~/apps/metroGen
          git fetch origin
          git reset --hard origin/main

      - name: Build & deploy staging
        run: |
          cd ~/apps/metroGen
          docker compose -f docker-compose.staging.yml up -d --build

      - name: Staging health check
        run: |
          for _ in {1..30}; do
            if curl -fsS http://127.0.0.1:9001/docs >/dev/null 2>&1; then
              echo "Staging API is ready"
              exit 0
            fi
            sleep 1
          done
          echo "Staging API failed to become ready within 30s"
          exit 1

      - name: Show staging status
        if: always()
        run: |
          cd ~/apps/metroGen
          docker compose -f docker-compose.staging.yml ps
